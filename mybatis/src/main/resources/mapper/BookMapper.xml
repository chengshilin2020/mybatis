<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datasouce.mybatis.mapper.BookMapper">

    <select id="getBookList" parameterType="java.lang.Integer" resultType="com.datasouce.mybatis.entity.Book">

--         select *
--         from BOOK
--         where 1=1
--         and count=#{count}
            call selectuser(?);
    </select>

</mapper>
